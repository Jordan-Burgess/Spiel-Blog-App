<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/show.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Lato:wght@300&family=Oleo+Script+Swash+Caps&family=Oswald:wght@200;500&family=Poiret+One&display=swap" rel="stylesheet">

<%- include('partials/head', {page: 'edit'})  %>
<%- include('partials/nav')  %>
    <title>Document</title>
</head>
<body>
    <h1><%= blog.title %></h1>
    </div>
    <div class ='blog-info'>
        <p class = 'blog-date'><%= blog.date %></p> 
        <p class = 'blog-user'><%=blog.user %></p>
    </div>
    <p class = 'blog-content'><%= blog.content %></p>
    <div class = 'blog-likes'>
        <p class ='number-of-likes'>Likes:   <%= blog.likes %></p>
        <form action="/blogs/<%= blog._id %>/like?_method=PUT" method="POST">
            <% blog.likes ++ %>
            <button type="submit">Like Post</button>
        </form>
    </div>

    <a href="/blogs/<%= blog._id %>/edit">Edit Post</a>
    <form action="/blogs/<%= blog._id %>?_method=DELETE" method="POST">  
        <input class="button is-danger" type="submit" value="Delete Blog"/>
    </form>


    <p class = 'add-comment'>Add Comment:</p>
    <% for(let i=0; i<comments.length; i++) { %>
        <p><%= comments[i].username %></p>
        <p><%= comments[i].content %></p>
        <p>Likes: <%= comments[i].likes %></p>
        <p><%= comments[i].date %></p>
        <hr>
    <% } %>
</body>
</html>