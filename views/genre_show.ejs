<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Genre</h1>
    <% for (let i=0; i<genres.length; i++) { %>
        <p><strong><%= genres[i].title %></strong></p>
        <p><%= genres[i].user %></p>
        <p><%= genres[i].content.slice(0, 40) %>...</p>
        <a>See More</a>
        <p><%= genres[i].date %></p>
        <hr>
    <% } %>
</body>
</html>