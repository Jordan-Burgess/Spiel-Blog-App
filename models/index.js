require("../config/db.connection");

module.exports = {
    Comments: require('./Comments')
}

require("../config/db.connection");

module.exports = {
    Blog: require('./Blog')
}

require("../config/db.connection");

module.exports = {
    User: require('./User')
}